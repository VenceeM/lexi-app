import { Router } from 'express'
import { login } from '../../services/auth/auth.js'

const router = Router();

export const login = async (req, res) => {
    try {
        

    } catch (error) {
        console.error(error);

    }
}